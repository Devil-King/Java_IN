//WAP to find out Number of integers which has exactly 9 divisors
import java.util.Scanner;
public class Main
{
public static void main(String[] args) {
    Scanner sc=new Scanner(System.in);
	int num=sc.nextInt();
	int count = 1;
	for(int i=1;i<=num/2;i++){
	 if(num%i==0){  
	 count++;
	}}
	if(count==9){
	   System.out.println(num+" which has exactly 9 divisors");  
	 }
	 else{
	  System.out.println(num+" which has not exactly 9 divisors");   
	 }
	
}}
